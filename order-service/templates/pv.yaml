apiVersion: v1
kind: PersistentVolume
metadata:
  name: order-mysql-pv
  namespace: order
spec:
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: local-storage
  local:
    path: /mnt/data/kubernetes/pv/order-mysql